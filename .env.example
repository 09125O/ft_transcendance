# -----------------------------------------------------------------------------
# Mode LAN:
# APP_PROTOCOL=http
# FRONTEND_ORIGIN=http://localhost:3000
# AUTH_COOKIE_SECURE=auto
# FT_REDIRECT_URI=http://localhost:4000/auth/42/callback
#
# Mode local HTTPS:
# APP_PROTOCOL=https
# FRONTEND_ORIGIN=https://localhost:3000
# AUTH_COOKIE_SECURE=true
# FT_REDIRECT_URI=https://localhost:4000/auth/42/callback
# -----------------------------------------------------------------------------

# -----------------------------------------------------------------------------
# Database
# -----------------------------------------------------------------------------
POSTGRES_USER=dev_user
POSTGRES_PASSWORD=dev_password
POSTGRES_DB=transcendance
POSTGRES_PORT=5432
DATABASE_URL=postgresql://dev_user:dev_password@db:5432/transcendance

# -----------------------------------------------------------------------------
# App runtime / network
# -----------------------------------------------------------------------------
APP_PROTOCOL=https
BACKEND_HOST=localhost
BACKEND_PORT=4000
FRONTEND_PORT=3000
FRONTEND_ORIGIN=https://localhost:3000

# -----------------------------------------------------------------------------
# Auth / session
# -----------------------------------------------------------------------------
JWT_SECRET=dev_jwt_secret
JWT_EXPIRES_IN=7d
AUTH_COOKIE_SAMESITE=lax
AUTH_COOKIE_SECURE=true

# -----------------------------------------------------------------------------
# Game timing
# -----------------------------------------------------------------------------
GAME_QUESTION_DURATION_MS=10000
GAME_ANSWER_GRACE_MS=400

# -----------------------------------------------------------------------------
# Room lifecycle
# -----------------------------------------------------------------------------
ROOM_RECONNECT_GRACE_MS=10000
ROOM_CLEANUP_INTERVAL_MS=30000
ROOM_WAITING_TTL_MS=1800000
ROOM_FINISHED_TTL_MS=300000

# -----------------------------------------------------------------------------
# Backup
# -----------------------------------------------------------------------------
BACKUP_INTERVAL_SECONDS=3600
BACKUP_RETENTION_COUNT=24

# -----------------------------------------------------------------------------
# OAuth 42
# -----------------------------------------------------------------------------
FT_CLIENT_ID=
FT_CLIENT_SECRET=
FT_REDIRECT_URI=https://localhost:4000/auth/42/callback
FT_SCOPE=public
OAUTH_HTTP_TIMEOUT_MS=10000
